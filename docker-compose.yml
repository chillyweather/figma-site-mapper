services:
  redis:
    image: redis:7-alpine
    container_name: fsm-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - fsm-network

volumes:
  redis-data:

networks:
  fsm-network:
    driver: bridge
